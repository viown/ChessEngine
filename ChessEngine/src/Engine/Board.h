#ifndef ENGINE_BOARD_H
#define ENGINE_BOARD_H
#include <vector>
#include "Position.h"

class Board {
private:
	//
public:
	Board(bool default_setup=true) {}

	void update_position(const Position& oldPos, Position newPos) {

	}
};

#endif